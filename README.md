
  # Global Design Tokens Setup

  This is a code bundle for Global Design Tokens Setup. The original project is available at https://www.figma.com/design/2VqQBXu01oOdaaPGRRnHPH/Global-Design-Tokens-Setup.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  