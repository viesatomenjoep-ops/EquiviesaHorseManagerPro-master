version: '3.8'

services:
  equiviesa:
    build: .
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - equiviesa-network

networks:
  equiviesa-network:
    driver: bridge
